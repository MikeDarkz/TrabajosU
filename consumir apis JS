<!DOCTYPE html>
<html lang="en-us">
  <head>
    <meta charset="utf-8">

    <title>Our superheroes</title>

    <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
  </head>

  <body>

      <header>

      </header>

      <section>

      </section>

    <script>

    async function populate() {

      const requestURL = 'https://thesimpsonsquoteapi.glitch.me/quotes?count=4';
      const request = new Request(requestURL);

      const response = await fetch(request);
      const simpsonCharacterText = await response.text();

      const simpsonCharacter = JSON.parse(simpsonCharacterText);
      populateHeader();
      populateSimpson(simpsonCharacter);
    }

    function populateHeader() {
      const header = document.querySelector('header');
      const myH1 = document.createElement('h1');
      myH1.textContent = 'Frases icónicas de Los Simpsons';
      header.appendChild(myH1);
    }

    function populateSimpson(obj) {
      const section = document.querySelector('section');
      for (const simpson of obj) {
        console.log(simpson);
        const myArticle = document.createElement('article');
        const simpsonImage = document.createElement('img');
        const myH2 = document.createElement('h2');
        const myPara1 = document.createElement('p');
        // const myPara2 = document.createElement('p');
        // const myPara3 = document.createElement('p');
        // const myList = document.createElement('ul');

        myH2.textContent = simpson.character;
        myPara1.textContent = `${simpson.quote}`;
        simpsonImage.src = simpson.image;
        simpsonImage.width = 200;
        simpsonImage.height = 200;
        // myPara2.textContent = `Age: ${hero.age}`;
        // myPara3.textContent = 'Superpowers:';

        
        myArticle.appendChild(simpsonImage);
        myArticle.appendChild(myH2);
        myArticle.appendChild(myPara1);
        // myArticle.appendChild(myPara3);
        // myArticle.appendChild(myList);

        section.appendChild(myArticle);
      }
    }

    populate();

    </script>
  </body>
</html>
---------------------------------------------------------------------------------------------------------------------------------------------------------
/* || general styles */

html {
  font-family: 'helvetica neue', helvetica, arial, sans-serif;
}

body {
  width: 800px;
  margin: 0 auto;
}

h1, h2 {
  font-family: 'Faster One', cursive;
}

/* header styles */

h1 {
  font-size: 4rem;
  text-align: center;
}

header p {
  font-size: 1.3rem;
  font-weight: bold;
  text-align: center;
}

/* section styles */

section article {
  width: 30%;
  float: left;
  margin: 10px;
  border: 1px solid rgb(237, 255, 77);
  border-radius: 5%;
  background-color: rgb(239, 248, 159);
}

section article img {
  background: linear-gradient(90deg, rgb(199 183 18) 0%, 
    rgba(231,255,63,1) 35%, 
    rgb(203 193 24) 100%);
  display: flex;
  margin: 0 auto;
  align-items: center;
  justify-content: center;
}

section p {
  margin: 5px 0;
}

section ul {
  margin-top: 0;
}

h2 {
  font-size: 2rem;
  letter-spacing: -2px;
  margin-bottom: 10px;
}
